## Simple Notetaking App

A minimal, offline-first notes app. Notes are stored in localStorage.

### Quick start
- Open `index.html` in a browser.

### Features (initial)
- Create, edit, delete notes
- Persist notes locally

### Files
- `index.html` — App shell
- `styles.css` — Basic styling
- `app.js` — App logic

# web
www
